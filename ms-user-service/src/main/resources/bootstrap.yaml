spring:
  application:
    name: ms-user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/user
    username: root
    password: mohamed123
  jpa:
    hibernate:
      ddl-auto: update