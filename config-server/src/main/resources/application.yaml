server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          default-label: local
          uri: https://github.com/thxmohamed/tingeso-prestabanco-microservicios
          search-paths: config-data

  application:
    name: config-server